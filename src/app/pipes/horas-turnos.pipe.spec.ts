import { HorasTurnosPipe } from './horas-turnos.pipe';

describe('HorasTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new HorasTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
