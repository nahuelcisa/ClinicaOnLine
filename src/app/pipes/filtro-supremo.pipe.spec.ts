import { FiltroSupremoPipe } from './filtro-supremo.pipe';

describe('FiltroSupremoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSupremoPipe();
    expect(pipe).toBeTruthy();
  });
});
