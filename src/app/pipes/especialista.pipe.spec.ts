import { EspecialistaPipe } from './especialista.pipe';

describe('EspecialistaPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialistaPipe();
    expect(pipe).toBeTruthy();
  });
});
