import { EspecialistasPipe } from './especialistas.pipe';

describe('EspecialistasPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialistasPipe();
    expect(pipe).toBeTruthy();
  });
});
